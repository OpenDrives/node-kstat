{
	"name":		"kstat",
	"version":	"1.0.1",
	"description":	"illumos libkstat bindings",
	"homepage":	"https://github.com/joyent/node-kstat",
	"author":	"Joyent (joyent.com)",
	"engines":	{ "node": ">=0.8" },
	"main":		"kstat",
	"dependencies": {
		"v8plus":	"^1.0.2"
	},
	"devDependencies": {
		"tape":		"4.6.2"
	},
	"scripts": {
		"postinstall": "node -e 'if(require(\"os\").platform() === \"linux\") process.exit(1)';  if [ $? -eq 0 ]; then gmake $(eval echo ${MAKE_OVERRIDES}); fi"
	}
}
